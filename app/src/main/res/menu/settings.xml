<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- "Mark Favorite", should appear as action button if possible -->
    <item
        android:id="@+id/action_create_habit"
        android:title="@string/action_create_habit"
        android:showAsAction="never"
        android:onClick="createHabitIntent"/>
    <item
        android:id="@+id/action_all_habits"
        android:title="@string/action_all_habits"
        android:showAsAction="never"
        android:onClick="allHabitIntent"/>



</menu>